query Client($id: Int!) {
  client(id: $id) {
    id
    firstName
    secondName
    lastName
    secondLastName
    birthDate
    email
    phone
    status
    assignedAnalyst
    createdAt
    updatedAt
  }
}
